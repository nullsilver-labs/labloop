# Session state — (never run)

No agent session has run yet. Session 1 checklist:

1. Read `AGENTS.md` (the operating manual), then `PLAN.md`, `CONSTRAINTS.md`.
2. Run `scripts/hwprobe.sh` to generate `HW.md`.
3. Write the initial `RESEARCH_NOTES.md` entry: your analysis of the plan, the risk
   ranking of the hypotheses, and your predictions.
4. Propose the first experiments as specs in `experiments/specs/` and register them
   in `experiments/LEDGER.md`.
5. Pause for human review before the first expensive run (unless `CONSTRAINTS.md`
   says otherwise).
6. Overwrite this file per AGENTS.md §5 before ending the session.
