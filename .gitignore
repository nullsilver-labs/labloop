# environments & caches
.venv/
venv/
__pycache__/
*.egg-info/
.pytest_cache/
.cache/
cache/

# large binaries — trial dirs stay in git, checkpoints don't
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.npz
*.arrow

# lab runtime state: session records, watch registry, metric throttle.
# Not evidence — population.json, events.jsonl and the candidate dirs are.
.lab/

# secrets
.env
*.key

# not ready to publish
WHITEPAPER.md
SPEC.md
SPEC_HAIKU.md
