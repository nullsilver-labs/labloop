# environments & caches
.venv/
venv/
__pycache__/
*.egg-info/
.pytest_cache/
.cache/
cache/

# large binaries — trial dirs stay in git, checkpoints don't
*.pt
*.pth
*.ckpt
*.safetensors
*.bin
*.npz
*.arrow

# lab runtime state: session records, phase session logs, metric throttle.
# Not evidence — state.json, events.jsonl and the trial dirs are.
.lab/

# secrets
.env
*.key
