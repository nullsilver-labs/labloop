# Experiment code lives here.

`code/expNN_<slug>.py` per experiment, shared helpers in `code/common.py`.
Written by the build phase, extended across runs; each trial pins the git commit
it ran at in its `config.json`.
